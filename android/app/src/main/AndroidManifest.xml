<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── PERMISOS NECESARIOS ── -->

    <!-- Micrófono (siempre escuchando) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Internet (Gemini API) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Servicio en segundo plano siempre activo -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Burbuja flotante sobre otras apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Vibración (retroalimentación al activar) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Llamadas -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Wake Lock (mantener pantalla / CPU activos) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Recibir arranque del sistema (auto-iniciar) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".VisionApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisionSoldier">

        <!-- ── ACTIVIDAD PRINCIPAL ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── SERVICIO PRINCIPAL (Micrófono + IA) ── -->
        <service
            android:name=".service.VisionForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- ── SERVICIO DE BURBUJA FLOTANTE ── -->
        <service
            android:name=".service.BubbleOverlayService"
            android:exported="false" />

        <!-- ── AUTO-ARRANQUE AL INICIAR EL DISPOSITIVO ── -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ── SERVICIO DE ACCESIBILIDAD (Control total del dispositivo) ── -->
        <service
            android:name=".accessibility.VisionAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

    </application>
</manifest>
